# apache-php-mysql-mssql-phpmyadmin

Docker && Apache && Php && Mysql (mysql || pdo_mysql) && Mssql (FreeTDS) && PhpMyAdmin

## Install

```bash
docker-compose up -d
```

## Document Root

```
www
```

## Server Root

```
localhost:8086
```

## PhpMyAdmin

```
localhost:8087
```
